# Database (Prisma Postgres dev proxy)
DATABASE_URL="prisma+postgres://localhost:5432/?api_key=YOUR_PRISMA_POSTGRES_API_KEY"

# JWT — change this to a strong random secret before deploying
JWT_SECRET="change-me-to-a-long-random-secret"
